<!--components/empty/empty.wxml-->
<view class="empty-wrap" wx:if="{{show}}">
  <text class="empty-icon">📭</text>
  <text class="empty-text">{{text}}</text>
  <text class="empty-sub" wx:if="{{subText}}">{{subText}}</text>
</view>
